from django.db import models

# 'auth.User 모델을 사용 => django 기본 제공 Model
# accounts.models 에서는 할일 x
# models => forms => urls + views + templates
# 우리가 사용할 회원가입/로그인 form 은 생성?
# django.contrib/auth.forms 에 다 준비되어 있음
# models(x) => forms(x) => urls + views + templates
